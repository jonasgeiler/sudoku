// 定义策略接口
export default class Strategy {
    /**
     * 应用策略
     * @param {number[][]} grid - 当前数独网格
     * @param {Object} candidates - 当前候选数字
     * @returns {Object} - { updated: boolean, steps: Array }
     */
    apply(grid, candidates) {
        throw new Error('Strategy.apply() must be implemented');
    }

    /**
     * 获取策略名称
     * @returns {string}
     */
    getName() {
        return 'Unnamed Strategy';
    }
}
