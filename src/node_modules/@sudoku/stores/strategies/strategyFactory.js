import { XWingStrategy } from './XWingStrategy';

export class StrategyFactory {
  static create(name) {
    switch (name) {
      case 'X-Wing':
        return new XWingStrategy();
      default:
        return null;
    }
  }
}