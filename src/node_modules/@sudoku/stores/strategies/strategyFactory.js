import { XWingStrategy } from './xWingStrategy';

class StrategyFactory {
  static create(name) {
    switch (name) {
      case 'x-wing':
        return new XWingStrategy();
      default:
        return null;
    }
  }
}