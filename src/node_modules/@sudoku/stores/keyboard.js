import { derived } from 'svelte/store';
import { grid } from '@sudoku/stores/grid';
import { cursor } from '@sudoku/stores/cursor';

export const keyboardDisabled = derived([cursor, grid], ([$cursor, $grid]) => {
	return cursor.isInitial($cursor) || !grid.isEmpty($grid, $cursor.x, $cursor.y);
});