import { writable } from 'svelte/store';

function createBacktrack() {
    const backtrack = writable(false);

    return {
        subscribe: backtrack.subscribe,

        toggle() {
            backtrack.update($backtrack => !$backtrack);
        }
    }
}

export const backtrack = createBacktrack();